#include <stdio.h>

int main(void) 
{
    int t=0, n, y, q;
    scanf("%d", &t);
    for (int i=t; i>0; i--)
    {
        scanf("%d", &n);
        int l[n];
        for (int j=0; j<n; j++)
        {
            scanf("%d", &y);
            l[j]=y;
        }
        q=l[0];
        for (int k=0; k<n; k++)
        {
            
        }
    }
	return 0;
}

