#include <stdio.h>
int main()
{
	int a;
	scanf("%d", &a);
	if (a==999)
	{
		printf("%d is largest three digit number", &a);
	}
	else
	{
		printf("%d is not largest three digit number", &a");
	}
	return 0;
}
